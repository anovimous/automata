@startuml
box "Worker Server"
participant "Execution Worker" as worker
end box
box "Central Server"
participant "Central Service" as api
database postgres
database redis
end box

autonumber "<b>[00]"

worker -> api : getRateAndStatus("RUN")
note right
In the current version,
all rates are constant
and immutable
end note
api -> postgres : getRunDetails("RUN")
postgres --> api : return run details
activate api

opt isStateQueued
api -> postgres : updateCurrentStateToRunning("RUN")
end
opt is current state other than requested
api -> postgres : updateCurrentToRequested("RUN")
end
opt isCurrentStateRunning
api -> redis : insertHeartbeatDate("RUN")
end
api --> worker : return rate and status


deactivate api


@enduml